name: Commit Checks
on:
  push:
    branches:
      - master
  pull_request:
jobs:
  lint:
    name: commitlint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: wagoid/commitlint-github-action@v4

