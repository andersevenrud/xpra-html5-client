name: LGTM Checks
on:
  push:
    branches:
      - typescript
  pull_request:
jobs:
  eslint:
    name: eslint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      - run: npm install -g yarn
      - run: yarn install --frozen-lockfile
      - run: yarn workspace xpra-ts lint
      - run: yarn workspace xpra-ts-ui lint
